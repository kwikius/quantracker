// Overview On
To make the OSD a library and move user customisations to user space

- Move the Sample-Dependencies.mk and Dependencies.mk to the quantracker root dir,
since the dependedncies apply globally

- Move the aircraft.cpp and aircraft.hpp to user space

- the Makefile in quantracker/air/osd just makes the OSD library.
To build a particular app use the Makefile in the user space